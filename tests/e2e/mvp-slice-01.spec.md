# MVP Slice 01 E2E Test Skeleton

Flow:
Lead → Discovery Call → Offer → Payment/Agreement → Onboarding → Manual/Commerce Provisioning → Coach Assignment → Journey/Cohort Assignment → Core Access Request → Delivery Plan → Member Home → First Step → Reflection → Coach Progress View → Message/Intervention → Renewal Trigger.

This file is a skeleton. Convert into Playwright/Cypress tests once implementation routes exist.
